import { Surbrillance } from './surbrillance';

describe('Surbrillance', () => {
  it('should create an instance', () => {
    const directive = new Surbrillance();
    expect(directive).toBeTruthy();
  });
});
